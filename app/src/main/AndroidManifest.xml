<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PasswordManager">
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Ustawienia"
            android:parentActivityName=".AccountListActivity"/>
        <activity
            android:name=".AccountManageActivity"
            android:exported="false"
            android:label="Edytuj konto"
            android:parentActivityName=".AccountListActivity" />
        <activity
            android:name=".AccountAddActivity"
            android:exported="false"
            android:label="Dodaj konto"
            android:parentActivityName=".AccountListActivity" />
        <activity
            android:name=".AccountListActivity"
            android:exported="false"
            android:label="Zarządzaj kontami" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>